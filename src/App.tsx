import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  padding: 5rem;
  text-align: center;
`;

function App() {
    return (
        <StyledWrapper>
            <h1>My App</h1>
        </StyledWrapper>
    );
}

export default App;
